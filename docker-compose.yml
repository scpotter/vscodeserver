services:
  code-server:
    build: .
    container_name: vscode-claude
    restart: unless-stopped
    command: --bind-addr 0.0.0.0:8080 /home/coder/repos
    environment:
      - PASSWORD=${CODE_SERVER_PASSWORD}
    volumes:
      - coder-home:/home/coder
    ports:
      - "8080:8080"

volumes:
  coder-home:
